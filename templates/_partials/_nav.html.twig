<nav>
    <ul>
	    <li><a href="{{ path('app_home') }}">Sonotown</a></li>

        {% if app.user %}

            {# Si l'utilisateur est admin #}

            {% if is_granted('ROLE_ADMIN') %}

            <div class="container"> 
                <nav class="navbar">
                    <li><a href="{{ path('admin_index') }}">Paramètre</a></li>
                    <li><a href="{{ path('show_profil')}}">Profil</a></li>  
                    <li><a href="{{ path('app_logout') }}">Logout</a></li>
                    <div class="hamburger">
                        <span class="bar">O</span>
                    </div>
                </nav>
            </div>          
            {% elseif is_granted('ROLE_ARTISTE')%}

            {# {% else %} #}

            {# Si l'utilisateur est artiste #}

                <li><a href="{{ path('all_artiste')}}">Artiste</a></li>
                {# <li><a href="{{ path('genre_musical')}}">Genres Musicaux</a></li> #}
                <li><a href="{{ path('artiste_all_album')}}">Albums</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
                {# <li><a href="{{ path('artiste_page_artiste')}}">Page de l'artiste</a></li>   #}
                <li><a href="{{ path('artiste_show_artiste')}}">Profil</a></li>  

            {% else %}

            {# Si l'utilisateur est user #}

                <li><a href="{{ path('artiste_all_artiste')}}">Artiste</a></li>
                {# <li><a href="{{ path('genre_musical')}}">Genres Musicaux</a></li> #}
                <li><a href="{{ path('artiste_all_album')}}">Albums</a></li>
                <li><a href="{{ path('show_profil')}}">Profil</a></li>  
                <li><a href="{{ path('app_logout') }}">Logout</a></li>

            {% endif %}
            
        {% else %}
              
            <li><a href="{{ path('artiste_all_artiste')}}">Artiste</a></li>
            {# <li><a href="{{ path('genre_musical')}}">Genres Musicaux</a></li> #}
            <li><a href="{{ path('artiste_all_album')}}">Albums</a></li>
            <li><a href="{{ path('app_register')}}">Register</a></li>    
            <li><a href="{{ path('app_login')}}">Login</a></li>    

        {% endif %}
    </ul>
</nav>